Implented deque and hashed deque in JavaScript. HashsedDeque allows for keys to be assigned to each node. 
These keys can be used to retrive data from or remove a node in O(1) time. HashedDeque extends the Deque class.
